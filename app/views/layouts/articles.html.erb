<!DOCTYPE html>
<html>
  <head>
    <title><%=Contest.find_by(active: 1).title%> - Help</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div>
      <%= render 'header'%>
    </div>
    
    <div class='botm-half-cntr'> <!-- This contains everything below the header-->
      <div><!-- This will be our sidebar-->
        <%= render 'sidebar'%>
      </div> 
      <div> <!-- This contains both the botton of the header and the main content of the page-->
        <div><!-- This will be our header bottom element-->
        
        </div> 
        <div>
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
