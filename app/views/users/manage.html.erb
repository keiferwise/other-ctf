<h2>users</h2>
<table>
  <tr>
    <th>Title</th>
    <th>score</th>
    <th></th>        
    <th></th>
    <th></th>
  </tr>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.login %></td>
      <td><%= user.email %></td>
      <td><%= link_to 'edit', edit_user_path(user) %></td>
      <td><%= link_to 'view', user_path(user) %></td>
      <td>Delete</td>
    </tr>
  <% end %>
</table>